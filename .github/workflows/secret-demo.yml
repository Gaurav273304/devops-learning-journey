name: Secret Demo

on: push

jobs:
  show-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Use secret
        run: echo "Secret is $MY_SECRET"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET_MESSAGE }}
